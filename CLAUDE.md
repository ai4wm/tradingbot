# CLAUDE.md

이 문서는 `trading-bot`의 구조 개편 원칙과 진행 상태, 다음 작업 순서를 기록합니다.

## 프로젝트 원칙

- 사용자에게는 존대어로 응답합니다.
- 현재 정상 작동하는 기능을 유지하면서 단계적으로 구조를 분리합니다.
- 구조 개편과 기능 변경을 한 단계에서 함께 처리하지 않습니다.
- 각 단계가 끝날 때 Python 문법 검사, import 검사와 주요 화면 실행 검증을 수행합니다.
- API 키, 계좌 정보, 텔레그램 세션과 `.env`는 Git에 커밋하지 않습니다.
- 실계좌 주문 기능을 변경할 때는 주문 수량, 중복 주문 방지와 비상 정지 동작을 우선 확인합니다.
- 검증에는 `.\.venv\Scripts\python.exe`만 사용합니다.
- 사용자 `layout.ini`와 운영 DB를 시험 목적으로 수정하지 않습니다. 시험은 임시 설정 파일로 진행합니다.

## 현재 구조

```text
main.py                  앱 제어, 창 수명주기, AnalysisWindow 뼈대, 분리 시계
ui/
├─ __init__.py           뉴스 탭 공용 상수(신규 표시 색상·역할)
├─ realtime_news_tab.py  LS 실시간 뉴스 탭, 상단 전광판, 본문 상세창
├─ stock_news_tab.py     네이버 종목뉴스 탭, 종목토론실
├─ telegram_news_tab.py  텔레그램 뉴스 탭, 원문 상세창
├─ limit_up_tab.py       상한가 탭
└─ theme_tab.py          테마 탭
telegram_news.py         Telethon 사용자 API 수집기, 종목 추출
ls_news_ws.py            LS 실시간 뉴스 수신
ls_news_server_sync.py   앱 종료 중 누락 LS 뉴스 보완
analysis_db.py           분석 SQLite 스키마와 조회·저장
```

`AnalysisWindow`는 Mixin 조합으로 구성합니다.

```python
class AnalysisWindow(
    LimitUpTabMixin, RealtimeNewsTabMixin, StockNewsTabMixin,
    TelegramNewsTabMixin, ThemeTabMixin, QMainWindow):
```

탭 코드는 파일만 분리한 상태이며 `self`를 통해 `AnalysisWindow`의 속성을 공유합니다. 완전한 위젯 분리와 Qt Signal 통신은 아직 적용하지 않았습니다.

## 완료된 작업

### 1. 현재 상태 보존 — 완료

기능 단위 커밋과 실행 기준점을 확보했습니다.

### 2. 뉴스 계층 분리(`news/` 패키지) — 보류

사용자 판단으로 건너뛰었습니다. 뉴스 공급처별 클라이언트는 각각 최상위 모듈(`ls_news_ws.py`, `naver_news_api.py`, `telegram_news.py`)로 유지하고, 저장·중복 방지는 `analysis_db.py`가 담당합니다. 공급처가 더 늘어나 중복이 커지면 다시 검토합니다.

### 3·5. 분석창 탭 분리 — 완료

`main.py`가 11,182줄에서 약 5,000줄로 줄었습니다. 뉴스 3개 탭과 상한가·테마 탭을 `ui/` 아래 Mixin으로 옮겼고, 도달하지 않는 화면 코드 52개 메서드를 함께 제거했습니다. 화면 모양과 동작은 바꾸지 않았습니다.

### 4. 텔레그램 뉴스 — 완료

- `TG_API_ID`/`TG_API_HASH` 기반 Telethon 사용자 API 연결, 전화번호·2단계 인증 처리
- 세션 파일 `data/telegram.session`, Git 제외
- 채널별 마지막 메시지 ID를 `telegram_news` 표에 저장하고 실행 시 소급 수집
- `(channel, message_id)` 조합으로 중복 저장 방지
- 탭 표시: 번호, 게시 시각, 채널명, 종목명·종목코드, 제목, 본문
- 채널·종목명·종목코드·본문 검색, 관심종목만 보기, 종목 있는 글만 보기
- 신규 강조와 `신규 해제` 버튼, 종목코드 유무에 따른 알림음 구분
- 종목 셀 좌클릭은 대표 종목코드 복사, 우클릭은 관심종목 추가 후 종토방 열기
- 행 클릭 시 앱 안 상세창에서 원문 표시(저장 본문을 먼저 보여 준 뒤 임베드로 교체), 창 크기와 글자 크기 저장
- 새 글은 분석창 상단 전광판에 `채널명 · 제목` 형태로 표시하며 소급 수집분은 제외

### 6. 분석창 시계 분리 — 완료

- 타이틀바 없는 반투명 창으로 분리하고 전체 영역 드래그, 가로·세로 자유 크기 조절, 휠 투명도 조절을 지원합니다.
- 분리 상태·위치·크기·투명도를 `layout.ini`에 저장하고 다음 실행과 분석창 재표시 때 복원합니다.
- 위치·크기는 400ms 모아서 저장하고, 창이 보이지 않는 동안에는 저장하지 않습니다. 숨김·종료 시 Qt가 보내는 좌표가 덮어쓰는 문제를 막습니다.
- 화면 밖 좌표로 저장되어도 보이는 위치로 되돌립니다.
- 분석창의 `📌 항상 위`와 분리 시계의 항상 위는 서로 영향을 주지 않습니다.
- 시계 표시는 날짜·요일, 시각, KRX·NXT 상태 배지까지입니다. 매매 국면 문구 줄은 제거했고 테두리·배경 색으로만 국면을 나타냅니다.

### 7. 뉴스 전광판·알림음·매수잔량 확대 창 — 완료

- 전광판은 공급처(LS·네이버·텔레그램)를 접두어 없이 배경·테두리 색으로 구분합니다. 색표는 `ui/realtime_news_tab.py`의 `LATEST_NEWS_BANNER_COLORS`입니다.
### 알림음은 종류별로 끄고 바꿉니다 (2026-09-18)

`소리` 체크는 전체 스위치 그대로 두고, 그 옆 `🔔` 버튼이 종류별 설정창을
엽니다(`rank.SoundSettingsDialog`). 거래소 공시는 하루 20여 건인데 기사는
4,000건이라 섞여 울리면 공시가 묻힙니다. 기사 알림만 끄고 공시만 남길 수
있어야 합니다.

13종류를 `rank.SOUND_LABELS` 순서대로 보여 줍니다. 제3자배정이 맨 위입니다.
줄마다 `켬` 체크, 현재 파일, `듣기`·`찾기`·`기본` 버튼입니다.

- 저장은 `layout.ini`의 `alert_sound/<종류>/{muted,file}`입니다.
- **끈 종류는 생성음까지 건너뜁니다.** 파일만 비우면 `TONES` 대체음이 울려
  껐다고 생각한 소리가 계속 납니다.
- **지정한 파일이 사라지면 기본 파일로 돌아갑니다.** 없는 경로를 물고 있으면
  그 종류만 조용히 안 울립니다.
- `듣기`는 껐어도 들립니다. 무슨 소리인지 확인하려고 누르는 버튼입니다.
- `SOUND_LABELS`에 없는 종류는 창에 안 뜨고 기본값 그대로 웁니다
  (`ls_news`·`telegram_news`는 옛 이름 호환용이라 뺐습니다).

- 뉴스 알림음은 **실시간 뉴스 탭의 `소리` 체크 하나**로 전부 켜고 끕니다
  (`news_sound_enabled`). 전에는 실시간·텔레그램에 체크가 따로 있어 한쪽을 꺼도
  다른 쪽이 울렸고, 네이버는 실시간 쪽 체크를 보고 있어 셋이 제각각이었습니다.
  텔레그램 탭의 `소리` 체크는 없앴습니다.
- 뉴스 알림음도 다른 알림과 같은 크기로 `winsound`가 재생합니다. 뉴스만 작게 내던 `rank.NEWS_ALERT_VOLUME`·`_play_wav_quiet`는 되돌렸습니다.
- 매수잔량 셀을 클릭하면 `gui.BidQtyPopup`이 종목별로 뜹니다. 타이틀바 없음, 항상 위, 드래그 이동, 우하단 크기 조절, 휠 투명도, 더블클릭 닫기, 조건 이탈 시 자동 닫기입니다.
- 확대 창은 매수잔량 아래에 누적거래량을 함께 보여 줍니다. 점상 대기 중에는 거의 늘지 않다가 무너지는 순간 튀므로 매수잔량과 짝이 됩니다. 글자 크기는 매수잔량의 40%, 색은 청록(`#6FD3C7`)입니다.
- 확대 창 갱신은 조회가 아니라 웹소켓 푸시가 직접 합니다. `0D` 호가의 `bid_qty`, `0B` 체결의 `vol` 중 하나가 실린 틱만 넘깁니다. 값은 `fields`가 아니라 역행 틱을 걸러 낸 뒤인 `model.rows`에서 읽습니다.
- 다시 그리는 일은 100ms로 묶습니다(`_paint_timer`). 체결 틱은 붕괴 순간 몰아치는데 같은 스레드에서 3단매도 판정이 돌기 때문입니다. `setStyleSheet`는 크기·투명도가 바뀔 때만 부릅니다(`_refresh`), 값 갱신은 `_refresh_text`만 합니다.
- 실제 주문을 보내는 `100주씩`·`분할매수` 버튼은 테두리 색으로 구분합니다. 파랑은 매도로 읽히므로 쓰지 않습니다.

### 8. 주문 유량과 3단매도 실계좌 검증 — 완료

2026-09-03. 상한가가 무너지는 순간 취소와 매도가 한 창에 나가도록 고쳤고,
전 경로를 실계좌로 확인했습니다.

주문 TR 유량 실측(문서는 "1초당 5회"만 적혀 있습니다).

- 버스트 천장은 8~10건입니다. 동시 발사면 10건, 시차가 생기면 9건입니다.
- 조회 TR과 창이 분리됩니다. 매도(`kt10001`)와 취소(`kt10003`)도 서로 다른
  버킷입니다. 취소 9건 + 매도 1건 동시가 세 번 모두 거부 0이었습니다.
- 회복은 건당 돌아옵니다. 10건을 채운 뒤 다음 1건이 0.14~0.47초,
  0.4초 뒤 5건 동시 재전송은 전부 튕겼습니다.
- 그래서 `ORDER_BURST` 5 → 10, `ORDER_RETRY_WAIT` 0.25 → 0.5,
  `ORDER_RETRY` 2 → 3으로 바꿨습니다. 취소 상한(`ORDER_BURST-1`)이 9가 되어
  9분할 취소와 전량매도가 같은 창에 들어갑니다.
- 마지막 안전장치인 스윕은 한 건이 실패해도 끝까지 돕니다. 이미 취소된
  주문(506550·571435·571412)은 조용히 넘깁니다.

3단매도는 상한가가 아니어도 직접 걸 수 있습니다. 걸어 둔 뒤 상한가가
무너져도 감시는 계속 도는데 거는 순간에만 막는 것이 앞뒤가 맞지 않았고,
상한가에서는 매수 자체가 안 되어 시험할 방법도 없었습니다. 상한가에서
걸었는지는 `at_upper`로 설정에 남아 감사 로그에 찍힙니다. 주문과 함께
나가는 자동 설정은 상한가 확인을 그대로 유지합니다.

실계좌 검증(023790, 1~2주):

| 경로 | 기록 |
| --- | --- |
| 1단 알림(0%) | 11:30:31 `stage skipped` |
| 2단 부분매도(50%) | 11:30:33 보유 2주 중 1주 |
| 2단 전량매도(100%) | 11:21, 11:26 |
| 3단 전량매도(100%) | 11:33:20 보유 2주 전량, 경계값 발동 |
| 취소 9건 + 매도 동시 | 3회, 전부 같은 순간 발사 |
| 유량 429 재전송 | 실제 발생 후 정상 복구 |
| 스윕 오류 내성 | 수정 뒤 세션 ERROR 0건 |

발동에서 매도 체결까지 191~242ms입니다. 같은 일이 09:49 엠아이큐브솔루션
에서는 1.12초였고, 그 사이 2회차 배분이 들어와 4건이 20주씩 더 체결됐습니다.

### 9. 9분할 매수 동시 전송 — 완료

2026-09-04. 동시호가 마감선은 랜덤이라 시가 결정 뒤에 도착한 주문은 배분에서
완전히 밀립니다. 그런데 `order.py`의 송신 워커가 한 건씩 응답까지 기다렸습니다.

- 09:01 에이프로젠바이오로직스: 9건에 986ms, 9번째는 유량 거부.
- 048770 실계좌 실측: 같은 9건 동시 전송은 78ms, 유량 거부 없음.
- 10:01 앱 적용 후 TPC로보틱스: 큐 투입부터 마지막 접수까지 198ms, 거부 0.
- 대기열을 통째로 비워 한 묶음으로 보냅니다. 전송 도중 남은 매수를 멈추는
  기능은 사라졌습니다. 동시호가 중에는 체결이 없어 취소할 것도 없고, 이미
  나간 주문은 9건 일괄취소로 정리됩니다(10:02 실측 91ms).

유량 장치는 둘로 보입니다(추론, 키움 미공개).

- 동시 진행 상한 약 10건. 12·15·20건을 던져도 통과는 정확히 10건입니다.
  30건은 전송이 길어져 앞 요청이 끝나며 17건이 통과했습니다.
- 초당 처리율 약 8건. 순차 전송은 진행 중 요청이 1건이라 위 상한에 안 걸리고
  이쪽에 걸립니다. 265ms에 9건(초당 34건)은 9번째가 튕겼고, 170ms 간격
  (초당 5.9건) 25건은 24건이 통과했습니다.

9분할 동시 발사는 두 장치 모두 아래입니다.

자동취소·3단매도·스윕은 `main.py`가 `rest`를 직접 부르므로 이 변경의 영향권
밖입니다. `order.py`를 거치는 경로는 9분할 매수와 주문 셀 취소 버튼뿐이고
둘 다 실계좌로 확인했습니다.

### 10. 3단매도 나머지취소 단계와 부분취소 장부 — 완료

2026-09-07. 사토시홀딩스(223310) 실계좌 첫 발동에서 나온 것들입니다.

**나머지취소 단계.** 3단매도 스피너에 `0% · 나머지취소`(비율 `-1.0`)를
넣었습니다. 매도 없이 `_trim_open_buys`만 부릅니다. 화면의 나머지취소
버튼과 같은 경로라 미체결 매수 각 건에서 100주만 남기고 부분취소합니다.

배분 장세에서 이 단계가 하는 일은 분명합니다. 점상 배분은 주문당 100주씩
들어오므로 1회차는 어차피 다 받습니다. 끊는 것은 2회차부터입니다.
09:58 실측으로 108주 8건 중 7건을 100주로 깎아 862주 대신 808주를 받았습니다.

취소가 나갔는지는 체결량이 아니라 **잔량**으로 봅니다. 배분에서는 모든 건이
100주로 찍히기 때문입니다. 깎인 7건은 `remaining=0`으로 끝났고, 취소가
실패한 `0006514`만 `remaining=8`이 남아 2·3회차에서 108주까지 채웠습니다.

단계별 비율은 마지막 선택을 기억합니다(`balance_sell_ratio_last/*`). 단계
체크·시장가와 같은 방식이고, 주문과 함께 걸리는 자동 3단매도도 그 값을
씁니다. 전에는 `0.0/1.0/1.0` 고정이었는데 마침 상용 설정과 같아 기억하는
것처럼 보였습니다.

**유량 첫 재시도 0.5 → 0.15초.** 취소 8건을 30ms 안에 동시 발사했는데 1건이
429였습니다. 재시도가 0.5초라 체결(429로부터 217ms)보다 늦었고, 그사이
그 주문이 108주 전량 체결됐습니다. 9-03 실측의 "0.4초 뒤 5건 동시 재전송은
전부 튕김"은 묶음 재전송 기준이고, 같은 실측에 "1건은 0.14~0.47초에 통과"가
있습니다. 그래서 첫 재시도만 짧게 가고 두 번째부터는 0.5초 그대로입니다.

429는 이 방식으로 바꾼 뒤 흔합니다. 9-04 이전 묶음이 거부 0이었던 것은
`order.py`가 순차 전송이라 1.1~2.0초에 걸쳐 나갔기 때문입니다(초당 8건).
동시 발사 표본은 아직 넷이고 셋이 통과, 하나가 1건 거부입니다.

**부분취소분이 체결로 잡히던 장부.** `_new_fill_qty`가 누적체결을
`주문수량 - 잔량`으로 셉니다. 그런데 부분취소를 해도 이벤트의 주문수량은
원주문(108) 그대로 옵니다. 100주 체결에 108로 세어 장부가 854주(실제 800)가
됐고 매도가 거부됐습니다. 재조회로 복구했지만 574ms를 썼습니다. 취소확인
이벤트의 주문수량이 곧 취소수량이므로 `_order_cancelled`에 누적해 함께
뺍니다. 거부된 취소는 확인 이벤트가 없어 아무것도 기록되지 않습니다.

"이미 취소·체결돼 못 했다"는 세 갈래(`open-buy cancel`, `trim cancel`,
`account auto-cancel`)를 `audit_log`로 옮겼습니다. 루트 레벨이 WARNING이라
`log.info` 갈래는 파일에 안 남아 결말을 볼 수 없었습니다.

**다 팔면 스스로 해제.** 켠 단계를 다 지나고 보유·미체결이 모두 비면
3단매도와 자동취소를 내립니다(`_clear_spent_balance_sell`). 부르는 자리는
둘입니다 — 매도 체결로 보유가 0이 되는 순간(`_track_open_sell`)과, 마지막
단계가 소진되는 순간(`_complete_balance_stage`)입니다. 뒤엣것이 없으면 한
주도 못 산 종목이 영영 안 내려갑니다. 매도 체결이 없어 앞 갈래에 닿지
않기 때문입니다(2026-09-08 224060: 1단 `stage skipped`, 2단
`consumed; nothing held`, 그 뒤 이벤트 없음). 소진된 설정이 남으면 같은 종목에 다시 들어갈 때가
문제입니다 — `auto_balance_sell_on_order`는 설정이 있으면 건드리지 않고
`_check_balance_sell`은 지나온 깊이를 다시 발동하지 않으므로, 화면에는
걸린 것처럼 보이는데 감시는 없는 상태로 재진입하게 됩니다. 장부를 세우기
전(`_position_book_primed`)이거나 매도가 도는 중이면 내리지 않습니다.

**재실행 복원.** 3단매도 설정(기준·비율·시장가)과 진행도, 자동취소 무장,
창별 주문상태를 그날 날짜로 `order/auto_state`에 저장합니다. 다만 **자동으로
켜지지 않습니다.** 계좌 줄의 `설정 복원` 버튼을 눌러야 감시가 돕니다.
2026-08-07에 저장 자체를 껐던 이유가 여기서 막힙니다 — 앱을 켠 순간 잔량이
기준선을 스치기만 해도 매도가 나가는 문제입니다. 좌클릭 복원, 우클릭 버림,
안 누르면 아무 일도 없고 다음 실행에 다시 뜹니다. 어제 저장분은 켤 때
버립니다. 버튼 자리는 쓰지 않던 `대출인출가능금액`을 빼고 그곳에 넣었습니다.

실계좌 기록(223310, 862주 9분할):

| 시각 | 일 |
| --- | --- |
| 09:58:18.528 | 잔량 842,253 → 1단 나머지취소 발동 |
| 09:58:18.83~18.86 | 부분취소 8건 동시, 7건 접수(54주), 1건 429 |
| 09:58:19.08~19.12 | 상한가 붕괴, 매수 808주 전량 체결 |
| 09:58:19.121 | 잔량 155,232 → 2단 전량매도 발동 |
| 09:58:19.188 | 장부 854주로 매도 거부(실제 800) |
| 09:58:19.762 | 재조회 후 800주 시장가 매도, 체결 |
| 09:58:19.805 | 남은 8주 매도, 체결 |

발동에서 체결까지 698ms입니다. 정상은 191~242ms이고, 늘어난 574ms가 장부
거부 때문이었습니다. 그 시각 잔량 소멸 속도는 초당 116만주라 남은 15만주는
0.13초치였습니다.

### 11. 상한가 후보 데이터 — 1단계 VI 원본 기록 — 진행 중

2026-09-08. 점상을 뺀 상한가 종목을 진입 시점부터 추적해 "가장 먼저 붙어서
안정화되는 종목"을 가려내려는 작업입니다. 그 첫 단계입니다.

**VI는 시장 전체가 옵니다.** 등록한 종목만 오는 줄 알았는데 아니었습니다.
같은 날 09:00~09:36 실측으로 미등록 535건, 등록 120건입니다. 그래서 실시간
등록 상한(`config.REAL_REG_LIMIT` 95)과 무관하게 화면 밖 종목의 VI를 봅니다.

**추적 시작점은 1차 정적VI입니다.** 상한가(+30%)를 가려면 +10% 정적VI를
반드시 지납니다. 안 걸리고 가는 것은 점상뿐이고 그건 제외 대상입니다.
같은 날 실측입니다.

| 종목 | VI 첫 등장 | 화면 편입 | 상한가 |
| --- | --- | --- | --- |
| 서전기전 `189860` | 09:00~09:01 | 09:05~09:06 | 09:08 |
| 액스비스 `0011A0` | 09:03~09:04 | 09:06~09:07 | 09:30경 |

VI가 상한가보다 7~8분, 조건검색 편입보다 2분 앞섰습니다.

**기록 형식.** `data/vi_raw_YYYYMMDD.jsonl`에 한 줄 한 건입니다.
`{"ts": "09:08:12", "code": "189860", "mine": true, "v": {원본 FID 전부}}`.
FID를 골라 담지 않습니다. 발동 이벤트와 해제 이벤트가 어떻게 다른지,
`9068`이 무엇인지 아직 모릅니다. 하루 2천여 건, 0.4MB라 압축하지 않습니다.

**1h FID 표.** 2026-09-08 09:39:38 `FID discover` 실수신(062970 한국첨단소재).
이 로그는 (type, fid)당 평생 한 번만 찍고 `bot.log`는 회전하므로 여기 옮깁니다.

| FID | 뜻 | 샘플 |
| --- | --- | --- |
| `9001` | 종목코드(`_AL` 접미사) | `062970_AL` |
| `302` | 종목명 | `한국첨단소재` |
| `1221` | VI 발동가격 | `2110` |
| `1223` | 발동 시각 | `093717` |
| `1224` | 해제 시각 | `093937` |
| `1225` | 적용구분 | `정적` / `동적` |
| `1236` | 정적 기준가격 | `1915` |
| `1237` | 동적 기준가격 | `0` |
| `1238` | 정적 괴리율 | `+10.18` |
| `1239` | 동적 괴리율 | `0.00` |
| `1279` | 방향 + 종류 | `+정적` / `-동적` |
| `9008` | 시장구분 | `101` |
| `1490` | 그날 그 종목의 VI 발동 횟수(상승·하락·정적·동적 통합) | `1` |
| `9068` | `1225`와 같은 값(정적/동적). **발동 플래그가 아님** | `1` |
| `9069`·`1489` | 미확인 | `1`, `+10.18` |

**발동과 해제는 `1224`로 가릅니다.** 발동 이벤트에는 해제 시각이 아직 없어
`000000`으로 옵니다(랜덤엔드라 그때는 정해지지도 않습니다). 2026-09-09 수신
1,114건에서 `9068`은 `1225`와 **100% 일치**했습니다(어긋난 건 0). `9068`을
발동 플래그로 쓰던 시절에는 **49%(556건)를 반대로 판정**했습니다 — 정적VI
해제 382건을 발동으로, 동적VI 발동 174건을 해제로 봤습니다. `ws.py`에 오래
적혀 있던 「07-09 007390 해제가 하루종일 미처리(expOFF zero 0건)」가
이것이었습니다. 판별은 `ws._vi_active`가 합니다.

**같은 이벤트가 두 번씩 옵니다.** 1,114건이 (코드, 발동시각, 해제시각, 차수)로
접으면 560건입니다. `_vi_first_time`이 콜백만 접습니다 — 그대로 흘리면 등록
종목마다 `_vi_fetch` 조회가 두 번 나갑니다. 원본 기록은 접지 않습니다. 두 벌이
늘 같지는 않습니다(범한퓨얼셀 09:00:14은 누적거래량만 `2447297`과 `1`로
달랐습니다). `1490`은 상승·하락과 정적·동적을 통합해 세므로 차수로
쓰려면 `1279`로 먼저 걸러야 합니다(신라에스지가 그날 7차까지 갔습니다).

**상승 VI 판별은 세 갈래로 서로 검증됩니다.** `1279`의 부호, `1238` 괴리율의
부호, 그리고 `1221 ÷ 1236`(2110 ÷ 1915 = +10.18%)입니다.

**차수도 `1236`으로 나옵니다.** 정적 기준가격은 VI마다 갱신됩니다. 1차는
기준가가 시가·전일종가, 2차는 1차 발동가입니다. 전일 종가 대비 `1236`이 이미
+10%면 2차입니다. 전일 종가는 로컬 DB에 있어 세지 않고 한 줄로 판별됩니다.

`1225`가 정적·동적 구분이라는 `ws.py`의 오래된 추측은 이것으로 확인됐습니다.
다만 `9068`이 무엇인지는 여전히 모릅니다(`1`만 관측). 발동·해제 시각이
`1223`·`1224`로 오므로 필요 없을 수도 있습니다.

수신 범위를 재던 `_survey_vi`는 답이 나왔고 이 파일이 대신하므로 지웠습니다.
bot.log의 `VI survey/60s` 줄(하루 30여 개)도 함께 사라집니다.

**2026-09-09 하루치 실측.** 그날 상한가 6종목이 **전부** 후보 안에 있었습니다.

| 종목 | 1차 VI | 2차 발동 | 2차 해제 | 상한가 | 여유 |
| --- | --- | --- | --- | --- | --- |
| 더코디 `224060` | 09:00:02 `+29.86%` | — | 09:02:11 | 09:04:17 | 2분 06초 |
| 범한퓨얼셀 `382900` | 09:00:14 `+29.24%` | — | 09:02:37 | 09:08:38 | 6분 01초 |
| 우리로 `046970` | 09:04:03 | 09:07:34 | 09:09:57 | 09:10:32 | **35초** |
| 머큐리 `100590` | 09:10:22 | 09:15:18 | 09:17:23 | 09:19:19 | 1분 56초 |
| 페니트리움 `187660` | 09:44:03 | 10:08:19 | 10:10:23 | 10:21:03 | 10분 40초 |
| 삼미금속 `012210` | 11:46:36 | 12:10:19 | 12:12:40 | 14:02:25 | 1시간 50분 |

기준가 갱신도 확인됐습니다 — 2차 정적 기준가가 1차 발동가와 같습니다
(우리로 `5190 → 5700`, 머큐리 `3350 → 3720`).

```
상승 정적VI 1회 이상        113종목
  ├ 2회 이상(계단식)         23종목  →  상한가 4
  └ 1차 괴리 +18.2% 초과      4종목  →  상한가 2
후보 합계                    27종목  →  상한가 6 (22%)
```

두 번째 갈래가 "시가 갭이 커서 정적VI 없이 직행"하는 무리입니다. 시가가
전일 종가 대비 +18.2%를 넘으면 다음 정적VI 발동가가 상한가를 넘어 안 걸립니다
(`시가 × 1.1 < 전일종가 × 1.3`).

**진입 창이 좁습니다.** 우리로는 2차 VI 해제 **35초** 뒤에 상한가였습니다.
해제를 보고 등록하면 늦습니다. 2차 VI가 **발동하는 순간** 등록해서 단일가
2분 동안 예상등락률을 봐야 합니다.

**남은 단계.**

1. VI 발동 → 상한가 진입 여부·소요시간을 20거래일 모으기. 잔량 없이도 됩니다.
2. 그다음에 1초 잔량 스냅샷. 어느 구간을 볼지 알고 나서 모읍니다.
   수집 창은 09:00~10:00, 그 시각에 상한가에 붙어 있는 종목만 붕괴 또는
   13:30까지 이어서. gzip으로 하루 7MB입니다. 09:30 컷은 안 됩니다 —
   같은 날 224060 붕괴가 09:31:01이었습니다.

표본이 100개는 넘어야 지표가 우연인지 갈립니다. 그전에 숫자로 매매하면
근거가 아닙니다.

## 다음 작업 순서

1. 시장·수급 분석과 공시·예측 영역 정리 — 남은 `main.py` 분석 코드의 실제 사용 여부를 먼저 확인한 뒤 이동 대상을 정합니다.
2. 뉴스 탭을 Mixin에서 독립 위젯으로 전환 — 상단 전광판, 알림음, 관심종목 상태를 Qt Signal 공통 이벤트로 바꿉니다.
3. 주문·잔고와 키움 실시간 등록 코드 검토 — 위 단계가 끝난 뒤 별도로 진행합니다.

## 공통 뉴스 데이터 권장 형식

```python
NewsItem(
    provider: str,
    external_id: str,
    channel: str,
    published_at: datetime,
    title: str,
    body: str,
    stock_codes: tuple[str, ...],
    url: str,
)
```

- 공급처별 원본 데이터는 클라이언트에서 공통 형식으로 변환합니다.
- UI는 공급처별 API 응답 구조를 직접 알지 않도록 합니다.
- DB 중복키는 `provider + channel + external_id`를 기본으로 사용합니다.

현재는 공급처별 표(`ls_realtime_news`, `telegram_news`)를 각각 사용하며, 위 형식은 통합 시점의 기준입니다.

## LS 뉴스 종목코드 형식

LS NWS는 종목코드를 12자리 고정 폭으로 이어 붙여 보냅니다. 앞 6자리는 `000000` 채움이고 뒤 6자리가 실제 코드입니다.

```text
0000000006600000000059300000000193T0
→ 000660, 005930, 0193T0
```

- 테마·업종 코드는 글자를 포함할 수 있으므로 숫자 여부로 분리하면 안 됩니다.
- 분리는 `analysis_db.split_ls_news_stock_codes`만 사용합니다.
- 종목명 표시는 이름이 확인되는 첫 종목을 대표로 사용합니다.

## 네이버 증권 화면 교체 (2026-09-11)

네이버 증권이 새 화면(`stock.naver.com` 계열)으로 바뀌면서 옛 DOM에 기대던
자바스크립트 주입이 전부 죽었습니다. 오류 없이 조용히 안 돌았습니다.

`_news_web_load_finished`에서 270줄을 지웠습니다(파일 1,191 → 960줄).

- **오늘글 강조** — `table tr`을 훑어 날짜 문자열이 든 행을 칠했습니다. 새
  목록은 `16분 전` 같은 상대 표기라 날짜 문자열 자체가 없습니다.
- **종목 중간 메뉴 스크롤 정렬** — `ul.tabs_submenu`를 찾아 그 위치로
  맞췄습니다. 새 화면에는 그 메뉴가 없습니다.

남긴 것은 **뉴스 기사 제목으로 스크롤**하는 갈래뿐입니다. 이쪽은
`finance.naver.com`이 아니라 기사 페이지라 그대로 동작합니다.

**가로 스크롤은 줌으로 맞춥니다.** 새 페이지가 좌우 패널을 달고 나와 요구
폭이 창보다 큽니다. 기준 폭을 코드에 박지 않고 로드 뒤 `scrollWidth`를 재서
`_fit_zoom`이 배율을 계산합니다. **줄이기만 하고 키우지 않아서** 같은
페이지를 두 번 재도 진동하지 않고, 이동할 때마다 `_show_news_web_url`이
1.0으로 되돌려 배율이 계속 작아지는 것을 막습니다. 최소 `0.5`에서 멈춥니다.
SPA는 로드 완료 뒤에도 내용이 채워지므로 0ms와 1200ms에 두 번 잽니다.

**자동 새로고침 판정**은 새 경로(`/domestic/stock/{코드}/discussion`)도
봅니다. 전에는 호스트를 `finance.naver.com`으로 고정 검사해서 새 주소에서
조용히 안 걸렸습니다. 확인한 경로만 넣습니다 — 뉴스·공시 주소를 확인하면
`NEWS_WEB_AUTO_RELOAD_TAILS`에 더합니다.

창 크기를 바꾸면 배율이 어긋난 채로 남습니다. 다시 열거나 자동 새로고침이
돌면 맞춰집니다.
**주소는 전부 새 형식으로 옮겼습니다.** 저장한 실제 페이지(프리티 006490)에서
확인한 것이라 추측이 없습니다. 조립은 `naver_stock_url()`이 합니다.

| 버튼 | 새 경로 | 옛 경로 |
| --- | --- | --- |
| 차트·시세 | `/price` | `sise.naver` + `fchart.naver` |
| 종목토론 | `/discussion?filter=all` | `board.naver` |
| 종목분석 | `/info` | `coinfo.naver` |
| 리포트 | `/research` | (새로 생김) |
| 뉴스·공시 | `/news` | `news.naver` + `dart.naver` |
| 공매도현황 | `/shortTrade` | `short_trade.naver` |
| 인사이트 | `/investmentinfo` | (새로 생김) |

9개가 7개가 됐습니다. 종합정보(`main.naver`)와 투자자별 매매동향
(`frgn.naver`)은 새 화면에 대응 탭이 없어 뺐습니다. 종목분석 아래에
`/info/company`(시세정보)와 `/info/investment`(투자정보)가 더 있습니다.

웹뷰 밖 세 곳(`gui.py` 조건검색 표, `rank.py` 조회순위,
`ui/limit_up_tab.py` 상한가 탭)은 종목토론 주소를 직접 적습니다. 서로 import
방향이 달라 헬퍼를 공유하지 않습니다.

## 상한가 정렬과 정리매매

정리매매 종목은 `_limit_tier`가 맨 아래(`TIER_PLAIN`)로 보냅니다. 가격제한폭이
없어 "상한가"라는 개념 자체가 없고, `upper`가 0이라 판정이 예상등락률만 보게
됩니다. 목록에서 숨기지는 않고 아래로만 내립니다.

2026-09-11 코스나인(082660)이 그 예입니다. 현재가 13원·시총 12억인데 실제
상한가 종목들 위에 하루 종일 앉아 있었습니다.

- 13원짜리는 한 호가가 1원, 즉 7.7%입니다. 하필 전일 종가와 같은 13원이라
  등락률이 **정확히 0.00**이 됩니다.
- 단일가매매 종목이라 예상값이 안 꺼집니다 — 끄는 조건이
  `code not in self.single`로 단일가를 아예 제외합니다.
- 그래서 `등락률 0 + 예상체결가 있음` = 장 시작 전 대기열(`TIER_PREOPEN`)에
  걸립니다. `_limit_tier` 주석이 "예상값은 체결이 재개되면 꺼진다"를 전제로
  둔 가드인데, 단일가 종목에서 그 전제가 깨집니다.

더 위험한 쪽은 따로 있었습니다. **17원이면 +30.7%**(4틱 위)라
`TIER_WAIT_CLEAN`으로 올라가 점상 대기 묶음에 들어가고 알림음까지 울립니다.

`model.liquidation`은 `main`이 주입합니다(`gui.py:1151`). `_limit_tier`는 dict만
받으므로 호출부 네 곳이 `code in ...liquidation`을 함께 넘깁니다.

## 주문가능수량은 수수료를 빼고 셉니다

2026-09-17. 보유종목을 판 직후 9분할이 **8건만** 나갔습니다.

```
09:00:09  미투온 900주 매도 체결 4,215원 → 대금 유입
09:00:15  cash_orderable 4,423,975원
09:00:49  앤씨앤 9분할 174주 × 9 = 1,566주 (= 4,423,950원, 남는 돈 25원)
09:00:50  9번째 거부 [2000](855056:매수증거금이 부족합니다. 173주 매수가능)
```

**키움이 주는 주문가능수량·금액(`kt00010`·`kt00001`)은 매수 수수료를 빼지
않은 값입니다.** 8건(1,392주)이 접수된 뒤 남은 증거금이 491,575원, 174주면
491,550원이라 들어갈 것 같은데 서버는 173주까지만 허용했습니다. 모자란 돈은
**664원**입니다 — 1,566주 전체로 보면 대금 4,423,950원에 수수료 0.015%인
664원을 더해 4,424,614원이 되어 4,423,975원을 넘습니다. 1,565주면
4,421,125 + 663 = 4,421,788원으로 들어가고, 이것이 서버가 말한 173주와
정확히 맞습니다. 2,850원은 나눌 수 없는 1주를 버리고 남는 돈이지 부족분이
아닙니다. 평소에는 계좌에 여윳돈이 있어 안 드러나고, **판 돈을
그대로 다시 쓰는 날에만** 나옵니다.

`_current_orderable_qty`가 수수료를 치르고 살 수 있는 수량을 셉니다.

```python
budget = min(api_qty * upper, remaining)
return int(budget / (upper * (1 + BUY_FEE_RATE)))
```

`BUY_FEE_RATE`는 0.05%입니다. 온라인 수수료 0.015% + 유관기관 제비용
0.0036%인데 계좌마다 달라 넉넉히 잡았습니다. 그날 값으로 1,566 → **1,565주**,
9분할이 `174×8 + 173`으로 딱 맞습니다. 마지막 건이 서버가 허용한 173주입니다.

**거부된 건을 줄여서 다시 내는 길은 쓰지 않습니다.** 처음부터 맞는 수량이
나가야 합니다. 재전송은 한 틱 늦고, 점상 배분에서 그 순서를 잃습니다.

딱 떨어지는 수보다 1주씩 적게 나옵니다 — 500주 가능이면 499주, 900주면
899주입니다. 100주씩 주문의 자투리도 그만큼 줄어듭니다(850 → 849, 자투리
50 → 49). 표시와 실제 주문이 같은 함수를 쓰므로 예상주문 줄도 같이 바뀝니다.

**화면에는 `오류`만 떴습니다.** 사유는 툴팁에만 들어가 동시호가 직후에는
읽을 시간이 없습니다. 8건이 접수된 상태였는데 전멸로 보여 전부 취소하고
영웅문에서 다시 넣었고, 그 사이 주문 순서가 32초 밀렸습니다(봇 8건 36ms,
영웅문 9건 4,795ms). 이제 접수분이 있으면 `자 8/9 오류`로 건수를 함께
보여 줍니다(`_on_order_update`).

## 공시는 LS가 이미 주고 있습니다

2026-09-18. DART 공시알람 앱을 합칠지 검토하다가, **봇이 이미 더 많이 더
빨리 받고 있다는 것**을 확인했습니다. LS `한국거래소` 출처가 그것입니다.

| | LS `한국거래소` | DART OpenAPI |
| --- | --- | --- |
| 일 건수 | 351건 | 145건 |
| 도달 | 실시간 푸시 | 10분 폴링 |
| 원문 | `t3102`가 DART 전문 HTML | 링크만 |

2026-09-15 미투온 실측입니다.

```
15:55:12 [한국거래소] (주)미투온 유상증자결정(제3자배정)   ← 공시
15:55:30 [이데일리]   미투온, 279억 규모 제3자배정 유상증자  ← 기사, 18초 뒤
16:00    애프터마켓 개장, 상한가 출발
```

**문제는 수신이 아니라 화면이었습니다.** 하루 4,000건 기사에 섞여 흐르고,
본문은 눌러야 옵니다 — 저장된 35,884건 중 본문이 있던 건 1건뿐이었습니다.

**한국거래소 출처는 전부 공시입니다.** 하루 351건이 종목 공시 223건,
ETF·ETN 기준가 안내 99건, 시장 안내 29건으로 갈립니다.

### 3단계로 가릅니다

| 단계 | 건/일 | 소리 | 행 배경 |
| --- | --- | --- | --- |
| 제3자배정 | 4.4 | `sound1.wav` | 빨강 |
| 그 밖의 재료급 | 19.0 | `sound6.wav` | 주황 |
| 나머지 공시 | 327.8 | 없음 | 옅은 보라 |

**제3자배정이 제일 셉니다.** 2026-05-26~09-18 실측으로 공시가 난 168종목 중
30종목(17.9%)이 5영업일 안에 상한가였습니다. 같은 기간 임의 종목·임의
시점의 기저확률이 1.2%이므로 **15배**입니다. 그 기간 2연상 이상 100건 중
18건이 이 종목들이었습니다(엑시온그룹·앱튼 4연상, 사토시홀딩스·미투온).
다만 "연상은 전부 제3자배정"은 아닙니다 — 서산(079650) 5연상은 아니었습니다.

`(정정)`은 뺍니다. 원본(3.7건/일)보다 정정(4.3건/일)이 많습니다.

**행 배경은 번호·종목명·출처 칸(0·2·4)에만 칠합니다.** 신규 강조가 시간·제목
칸(1·3)을 쓰고 `신규해제`가 그 둘을 지우므로, 거기 칠하면 같이 날아갑니다.

**숨기지 않고 칠합니다.** 필터로 공시만 켜면 기사가 사라지는데, 미투온 때
공시와 기사가 18초 차이로 붙어 나왔습니다. 둘 다 봐야 합니다. 출처 필터가
필요하면 검색창이 이미 뉴스출처 칸을 봅니다(`한국거래소 -ETF -ETN`).
검색창 옆 즐겨찾기 드롭다운과 `★` 버튼이 그 식을 기억합니다.

**`★`은 넣기만 합니다.** 토글로 뒀더니 즐겨찾기를 고른 직후 검색창에 그 식이
그대로 들어가 있어서, 저장하려고 누른 `★`이 그것을 지웠습니다(2026-09-18에
4개가 2개로 줄었습니다). 빼기와 `기본 즐겨찾기 되살리기`는 목록 우클릭으로
옮겼습니다 — 청산키 칸·`설정 복원` 버튼과 같은 관용구입니다.

**펼친 목록만 글자 길이에 맞춰 넓힙니다.** 닫힌 칸은 150px이라 긴 식이
잘리는데, 닫힌 칸까지 늘리면 주문줄이 밀립니다.

### 재료급은 본문을 미리 받습니다

`_schedule_disclosure_prefetch`가 `t3102`로 본문을 받아 DB에 넣습니다.
하루 20여 건이라 부하가 없습니다. 발행가·주식수·납입일이 공시 도착
시점에 이미 DB에 있게 됩니다.

### 본문을 채점합니다 (`disclosure.py`)

제목만으로는 강도를 모릅니다. 같은 「유상증자결정(제3자배정)」인데도
그렇습니다.

| | 앤씨앤 09-16 | 미투온 09-15 |
| --- | --- | --- |
| 희석률 | 79.7% | 30.0% |
| 조달/시총 | 91.6% | 28.9% |
| 최대주주 변경 | O | O |
| 납입일 | 12일 뒤 | 62일 뒤(공정위 심사) |
| **점수** | **93** | **63** |

**라벨 철자를 믿지 않습니다.** 본문이 EUC-KR로 오면서 글자가 깨지는데
**깨지는 자리가 종목마다 다릅니다.** 같은 이틀 사이에 앤씨앤은
`4. 자금조달�� 목적`이고 미투온은 멀쩡했습니다. 그래서 큰 항목 번호가
아니라 안 깨진 하위 라벨(`운영자금 (원)`)에 겁니다.

**`-`(해당없음) 칸에서 멈춥니다.** 안 멈추면 다음 항목 금액을 제 것으로
집어옵니다 — 앤씨앤 시설자금이 `-`인데 그 뒤 운영자금 100억을 가져와
시설투자 공시로 둔갑했습니다.

**종가를 모르면 조달/시총만 0점입니다.** 추정치로 채우면 실제와 다른 값이
점수에 들어갑니다. `analysis_db.last_close_price`가 없으면 0을 돌려줍니다.

배점(`DILUTION_STEPS` 등)은 손으로 잡은 값입니다. 표본이 쌓이면 실제 연속
상한가 일수로 회귀해 다시 잡습니다.

### 아직 아닌 것

- 인수자 규모. 본문에 「법인 또는 단체의 최근 결산기 주요 재무사항」으로
  자산총계·매출액이 있습니다(카카오게임즈 자산 2.7조·매출 4,650억).
  대상자가 여럿이면 표가 반복돼 파싱이 한 겹 더 필요합니다.
- 중복 공시. 같은 건이 다른 `realkey`로 두 번 올 때가 있어 소리가 두 번
  납니다(2026-09-17 17:26 모티브링크, 09-08 17:34·17:39 광진실업).

## 청산은 주문허용 체크를 보지 않습니다

2026-09-11. 3단매도·자동취소·스윕은 원래 주문허용을 안 봅니다
(`_order_enabled`가 그 경로에 한 군데도 없습니다). 청산키만 혼자 보고
`허용꺼짐`으로 막았습니다. 앞뒤가 안 맞고, 비상 장치가 체크박스 때문에 안
나가는 쪽이 더 큰 손해라 막지 않기로 했습니다.

`_emergency_exit`·`_queue_emergency_reconcile`·`_emergency_exit_async`에서
`order_enabled` 인자를 뺐습니다. 청산키 발동 로그에는 그때의 주문허용 상태를
`enabled=`로 계속 남깁니다.

## 청산 속도와 뒤처리

청산은 3단매도의 100% 전량매도와 하는 일이 같습니다 — 미체결 취소 + 전량
매도. 그런데 속도와 뒤처리가 달랐습니다.

**속도.** `_emergency_exit_async`는 `_position_book`에 **항목이 있을 때만**
장부를 믿고, 없으면 계좌조회 두 건을 기다렸습니다. 장부 항목은 체결이 있어야
생기므로, 미체결만 있고 아직 체결은 없는 상태 — 청산키를 가장 많이 누를 그
순간 — 이 정확히 가장 느렸습니다.

```
2026-09-11 10:39:23.275  청산키 F1 눌림
2026-09-11 10:39:24.360  취소 발사          +1,085ms
```

3단매도 정상이 191~242ms입니다. 조회 큐가 1초에 1건이라 두 건이 그대로 1초로
붙었습니다. 같은 날 첫 시도(10:38)는 반응이 없어 2.4초 뒤 한 번 더 눌렀고
1차 기준 4,027ms가 걸렸습니다.

이제 `_position_book_primed`가 서 있으면 **항목이 없는 것도 보유 0으로
믿습니다.** `_check_balance_sell`이 쓰던 규칙과 같습니다.

**뒤처리.** 청산으로 끝내면 3단매도·자동취소·청산키가 안 내려갔습니다.
`_clear_spent_balance_sell`이 `진행도 >= 켠 단계 수`를 요구하는데 청산은
단계를 지나지 않아 진행도가 0이고, 매도 체결도 없어 그 갈래에 닿지도
않습니다(2026-09-11 006490·007540, 둘 다 손으로 내렸습니다).

`_clear_after_emergency`가 합니다. **청산키를 누른 행동이 기준**이라 진행도도,
주문허용 상태도 묻지 않습니다. 관문은 둘입니다: 장부를 세웠을 것,
보유·미체결이 0일 것.

**부르는 자리가 셋**입니다. 청산 작업이 끝났을 때만 부르면 안 됩니다 —
취소확인이 그보다 늦게 와서 미체결 관문에 걸립니다(2026-09-11 10:56 006490:
취소 발사 +1ms, 취소확인 +71ms).

```
_finish_emergency_reconcile   청산 작업이 끝났을 때
_on_account_order_event       매수 취소확인·체결로 장부가 빈 뒤
_track_open_sell              청산 매도가 체결돼 보유가 0이 된 뒤
```

뒤의 둘은 `_emergency_locked`인 종목만 부릅니다. 그리고 남은 것이 없다고
판정하는 순간 그 표식을 내립니다 — 안 내리면 하루 종일 남아서, 같은 종목에
새로 건 3단매도가 나중에 매수 하나 취소될 때 휩쓸립니다.

## 청산 가격은 하한가 지정가

`_emergency_exit_price`가 정합니다. **시장가가 아닙니다.** 하한가로 내도 체결은
매수 호가를 위에서부터 훑으므로 결과가 시장가와 같고, 하한가까지 긁히는 것은
호가가 실제로 빈 경우뿐입니다. 그때는 시장가도 마찬가지입니다.

다른 점이 둘입니다.

- **애프터마켓에서 나갑니다.** 2026-09-14부터 KRX 시간외단일가가 폐지되고
  16~20시 애프터마켓(접속매매)이 열렸는데, 여기는 시장가를 받지 않습니다.
  지정가·최우선지정가·최유리지정가만입니다.
- **`bid_price4`에 매달리지 않습니다.** 전에는 매수 4호가로 냈습니다.
  점상이 무너져 호가가 증발하는 바로 그 순간 값이 0이 되어
  `매도 지정가는 0원보다 커야 합니다`로 거부됐습니다. 청산키가 가장 필요한
  순간입니다. 3단매도 3단에는 `_balance_stage3_limit_price`라는 보정이 있어
  같은 상황에서 그쪽만 나갔습니다.

하한가는 편입 조회(`lst_pric`)로 들어와 행에 남습니다. 없으면 행에 실린 마지막
매수호가에 냅니다(`_deepest_bid`) — 이쪽은 웹소켓이 따로 채우므로 조회가 안 붙은
행에도 있습니다. 호가가 하나도 없을 때만 상한가 기준 3틱 아래를 계산합니다.
전일종가로 물러나는 길은 없습니다 — `base`와 `lower`가 같은 조회에 실려 오므로
하한가가 없으면 전일종가도 없습니다.

## 3단매도 감시 국면

`_check_balance_sell`의 관문이 `정규장`만 보던 것을 `BALANCE_SELL_SESSIONS`로
바꿨습니다 — `정규장`·`종가 동시호가`·`애프터마켓`입니다. 호가잔량은 세 구간
모두 웹소켓으로 들어오는데 코드가 막고 있었습니다.

| 시각 | KRX | 감시 |
| --- | --- | --- |
| 09:00~15:20 | 정규장 | O |
| 15:20~15:30 | 종가 동시호가 | O |
| 15:30~15:40 | 정규장 종료 | X |
| 15:40~16:00 | 시간외종가 | X |
| 16:00~20:00 | 애프터마켓 | O |

**종가 동시호가**는 그 10분에 상이 무너지면 미체결 매수가 그대로 종가에
체결되기 때문입니다. 발동하면 평소처럼 미체결을 끊고 매도가 나가는데,
동시호가라 체결은 15:30 종가에 한꺼번에 됩니다.

**애프터마켓**은 16시에 호가창이 백지에서 시작합니다. 정규장 기준선을 그대로
두면 첫 틱에 `bid_qty=0`으로 2단이 바로 걸립니다. 그래서 15:33에 정규장 설정을
내리고, 저녁에 쓰려면 잔량이 쌓인 것을 보고 기준선을 새로 잡아야 합니다.

**시가 동시호가와 시간외종가는 넣지 않았습니다.** 앞은 9분할 매수가 나가는
구간이라 자기 주문에 반응하고, 뒤는 종가 한 값으로만 거래됩니다.

## 장이 끝나면 자동 해제 (15:33 · 20:05)

`_on_session_cleanup`이 3단매도와 자동취소를 전부 내립니다. 한 번만 울리는
`QTimer`라 그때까지 아무 일도 하지 않고, 울리면 다음 날치를 다시 겁니다.

감시 구간이 끝난 뒤마다 한 번씩입니다(`SESSION_CLEANUP_TIMES`). 종가는 15:30,
애프터마켓은 20:00에 끝나는데 체결 통보가 몇 초 늦게 오므로 3분과 5분을 둡니다.
15:33 해제는 정규장 기준선이 16시 애프터마켓으로 넘어가지 않게 하는 안전장치도
겸합니다.

설정이 남으면 화면에는 걸린 것처럼 보이고, 다음 날 `auto_balance_sell_on_order`
가 새 잔량 기준으로 다시 걸지도 않습니다(설정이 있으면 건드리지 않습니다).

**미체결 매수는 건드리지 않습니다.** 종가 동시호가에 그대로 들어갑니다.
무너지면 3단매도가 끊고, 안 되면 15:30에 거래소가 지웁니다.

**청산키는 내리지 않습니다.** 애프터마켓(16~20시)에서 손쓸 수 있는 수단이
그것뿐입니다.

## 호가는 10단계를 다 받습니다

2026-09-14. `0D`는 10단계를 다 보내는데 5단계까지만 매핑하고 나머지를 버리고
있었습니다. `FID discover`가 `46~50`·`56~60`·`66~70`·`76~80`을 UNMAPPED로
계속 찍고 있던 것이 그것입니다.

| FID | 뜻 |
| --- | --- |
| `41`~`50` | 매도호가 1~10 |
| `51`~`60` | 매수호가 1~10 |
| `61`~`70` | 매도호가 잔량 1~10 |
| `71`~`80` | 매수호가 잔량 1~10 |

`gui.BOOK_FIELDS`와 15초 호가 이력(`self.quotes`)도 10단계로 늘렸습니다.
REST(`ka10095`)는 5단계 그대로입니다 — 6~10단을 주는지 확인하지 않았고, 안 주는
것을 넣으면 0이 웹소켓 값을 덮습니다. 편입 직후 한 틱 동안만 6~10단이 비고
그다음 호가 틱이 채웁니다.

**가격은 `_emergency_exit`이 정합니다.** 전역키(`_on_global_exit_hotkey`)와 창 안
키(`gui.eventFilter`)가 각자 계산하던 것을 한 곳으로 모았습니다. 부르는 쪽이
넘긴 값은 행을 못 찾을 때만 씁니다.

**3단매도의 `시장가 매도` 체크도 하한가 지정가로 바꿨습니다.** 화면 이름은
`하한가 매도`이고 저장 키는 `market_sell` 그대로입니다(재실행 복원 호환).
체크를 안 하면 전과 같이 1·2단은 상한가, 3단은 4호가 지정가입니다.

**하한가도 호가도 끝내 모를 때만 시장가로 물러납니다.** 안 나가는 것이 제일
나쁩니다. 청산에는 이 갈래가 없습니다.

3단매도는 종가 동시호가(15:30)까지 돌고, 16시 애프터마켓에서 다시 돕니다.
다만 15:33에 설정이 내려가므로 저녁에 쓰려면 새로 걸어야 합니다.

## 청산키 자동 배정

주문이 나갈 때 청산키를 자동으로 겁니다(`_auto_assign_exit_hotkey`,
3단매도 자동 설정 바로 옆). 3단매도·자동취소는 이미 주문과 함께 걸리는데
청산키만 손으로 걸어야 했고, 잊으면 앱이 뒤에 있을 때 손쓸 방법이 없습니다.

후보는 앞에서부터 빈 키를 집습니다.

```
F1 → F5 → F9 → F2 → F6 → F10
```

쓰던 키가 풀리면 다음 주문은 다시 앞쪽을 집습니다. 보통 세 개를 넘지
않습니다. 다른 프로그램이 잡고 있어 `RegisterHotKey`가 거부하면 다음 후보로
넘어갑니다. **다만 키보드 후킹 방식 매크로는 거부되지 않습니다** — 그런 키를
쓰고 계시면 `AUTO_EXIT_HOTKEY_LABELS`에서 빼야 둘이 같이 발동하지 않습니다.
`F12`는 윈도우 예약키라 후보에서 뺐습니다.

**해제는 청산키 칸 좌클릭 한 번**입니다. 자동으로 걸리니 손으로 하는 일은
거의 푸는 쪽입니다. 비어 있는 칸을 누르면 전처럼 키 입력을 기다립니다.

**잔고가 비면 같이 내려갑니다.** `_clear_spent_balance_sell`이 3단매도·
자동취소와 함께 `_clear_exit_hotkey`를 부릅니다. 다 판 종목의 키가 살아
있으면 나중에 눌렀을 때 엉뚱하게 동작합니다. 다시 주문하면 새로 걸립니다.

## 청산키 전역 등록

`RegisterHotKey` 기반이라 앱이 뒤에 있어도 먹습니다. 다만 **F12는 어떤
경우에도 등록되지 않습니다.** 윈도우가 디버거용으로 영구 예약합니다 —
디버깅 중이 아니어도, 관리자 권한이어도 실패합니다(2026-09-09 224060 F12
거부, 같은 세션 30초 전 F9는 성공).

등록이 거부되면 **칸에서 키를 빼고** 주문상태 칸에 `키충돌`을 띄웁니다.
칸에 키가 남으면 앱이 뒤에 있을 때 안 먹는 줄 모릅니다. 청산키는 바로 그때
쓰는 물건입니다. gui는 신호를 보낸 뒤 배정이 살아남았는지 보고 안내 문구를
가릅니다(같은 스레드라 `emit`이 돌아온 시점에 결과가 나 있습니다).

`키충돌`은 주문 상태가 아니라 알림이므로 저장·복원에서 걷어냅니다
(`_drop_hotkey_conflicts`). 다시 등록에 성공하거나 청산키를 해제하면
지웁니다. 셋 다 없던 시절에는 한 번 뜨면 켤 때마다 되살아났습니다.

## 네이버 뉴스와 테마

네이버 뉴스는 **제목에 종목명이 그대로 있을 때만** 테마에 붙입니다
(`analysis_db.save_news_items`와 `backfill_news_themes` 양쪽에 같은 조건).

종목명으로 검색해 가져오므로 요약문에 이름만 스친 시황 기사가 잔뜩 딸려
옵니다. 2026-09-07~09 실데이터에서 테마가 잡힌 47건 중 33건이 그런 것이었고,
"원전주 급등" 마감시황이 더코디에 `원자력`을, "반도체 장비주" 시황이
케이엠제약에 `반도체장비`를 붙이는 식이었습니다. `confidence` 0.9로는 못
거릅니다 — 제목이 아니라 제목+요약을 보고 매기므로 종목명이 나열된 시황
기사도 0.9가 됩니다. 제목으로 자르면 14건만 남고 오염이 사라집니다.

두 곳 중 한쪽만 고치면 안 됩니다. 소급(`backfill_news_themes`)은 "지금
사전으로 재현되지 않는 NEWS 연결"을 지우므로, 소급이 네이버를 안 보면 저장
때 붙인 연결을 도로 지웁니다. `test_naver_news_theme.py`가 그 회귀를 봅니다.

이 규칙 전에는 네이버 뉴스를 테마에 아예 안 썼습니다. 그래서 LS가 안 받는
매체의 기사는 테마에 안 잡혔습니다(2026-09-09 08:16 nbntv 「[풍문레이다]
범LG가 3세 구본호, 더코디 품는다」).

## 단계별 검증

```powershell
.\.venv\Scripts\python.exe -m py_compile (Get-ChildItem *.py, ui\*.py | ForEach-Object FullName)
.\.venv\Scripts\python.exe telegram_news.py
git diff --check
```

`test_*.py`는 각각 단독 실행하는 자체 검사입니다. 프레임워크를 쓰지
않으며 성공하면 `ok`만 찍습니다.

```powershell
Get-ChildItem test_*.py | ForEach-Object { .\.venv\Scripts\python.exe $_.Name }
```

추가로 확인할 항목:

- 분석창을 숨겨도 활성화된 뉴스 알림음이 유지되는지 확인
- LS 종목코드 있음·없음 사운드가 구분되는지 확인
- 네이버 신규 뉴스 사운드와 상단 전광판 표시 확인
- 종목토론 웹뷰에 가로 스크롤바가 안 생기는지, 자동 새로고침이 새 주소에서도 도는지 확인
- 네이버 뉴스로 붙은 테마가 제목에 종목명이 있는 기사만인지, 소급 버튼을 눌러도 안 지워지는지 확인
- 텔레그램 새 글의 전광판 표시와 알림음 구분 확인
- 실시간 뉴스 탭의 `소리`를 끄면 네이버·텔레그램 알림음도 같이 멎는지 확인
- 앱 재시작 후 뉴스 중복 저장 및 누락 보완 확인
- 본창 재조회 체크가 오전 09:02:20에 자동 해제되는지 확인
- 추가 조건검색창의 재조회 체크에는 영향을 주지 않는지 확인
- 시계를 분리한 채 종료한 뒤 재실행과 분석창 재표시에서 위치·크기가 복원되는지 확인
- 첫 뉴스 알림(파일별 최초 재생)이 실제로 소리가 나는지 확인
- 상한가 정렬에서 정리매매 종목이 실제 상한가 아래에 있는지, 점상 구분선 안에 안 들어오는지 확인
- 매수잔량 확대 창의 값 갱신, 더블클릭 닫기, 조건 이탈 자동 닫기 확인
- 확대 창에서 매수잔량과 누적거래량이 함께 흐르는지, 창을 가장 작게 줄여도 세 줄이 안 잘리는지 확인
- 3단매도 스피너에서 고른 비율이 다음 설정창과 자동 3단매도에 그대로 오는지 확인
- 나머지취소 단계 뒤 매도가 거부 없이 한 번에 나가는지 확인(장부 부풀림)
- 보유종목을 판 직후 9분할이 9건 다 나가는지 확인(수수료 몫)
- 분할 중 한 건만 실패했을 때 주문상태가 `자 8/9 오류`로 뜨는지 확인
- 제3자배정 공시에 전용 알림음이 울리고 전광판이 빨강으로 바뀌는지 확인
- `신규해제`를 눌러도 한국거래소 행의 배경색이 남는지 확인
- 재료급 공시 뒤 상태바에 `공시강도`가 뜨고 bot.log에 남는지 확인
- 알림음 설정창에서 한 종류를 끄면 그것만 안 울리고 나머지는 우는지 확인
- 알림음 파일을 바꾼 뒤 앱을 다시 켜도 그 파일이 유지되는지 확인
- 즐겨찾기를 고른 뒤 `★`을 눌러도 그 식이 지워지지 않는지 확인
- 전량매도 뒤 3단매도·자동취소 칸이 비고, 같은 종목 재매수에서 다시 걸리는지 확인
- 한 주도 못 사고 미체결이 전부 취소된 종목도 마지막 단계 소진과 함께 칸이 비는지 확인
- 청산키를 F12로 걸면 칸이 비고 `키충돌`이 뜨는지, 다른 키로 다시 걸면 사라지는지 확인
- 주문을 내면 청산키가 F1부터 자동으로 걸리는지, 칸을 좌클릭하면 한 번에 풀리는지 확인
- 전량매도로 잔고가 비면 3단매도·자동취소와 함께 청산키도 내려가는지 확인
- 종가 동시호가(15:20~15:30)에 잔량이 기준선을 밑돌면 3단매도가 도는지 확인
- 15:33에 3단매도·자동취소가 비고, 미체결 매수와 청산키는 남는지 확인
- 16시 이후 잔량을 보고 새로 건 3단매도가 실제로 도는지, 20:05에 비는지 확인
- 청산키로 끝냈을 때도 셋 다 내려가는지, 미체결만 있는 상태에서 계좌조회 없이 바로 취소가 나가는지 확인
- 재실행 때 `설정 복원` 버튼이 뜨고, 누르기 전에는 감시가 돌지 않는지 확인
- 복원도 무시도 하지 않은 채 두 번 재시작해도 저장분이 남아 있는지 확인

## 하지 말아야 할 작업

- 구조 개편 중 기존 주문 로직을 동시에 재작성하지 않습니다.
- 기존 DB를 삭제하거나 전체 재생성하지 않습니다.
- 사용자 `.env`, `layout.ini`, 운영 DB와 세션 파일을 덮어쓰지 않습니다.
- 리팩터링이 끝나기 전에 기존 파일을 먼저 삭제하지 않습니다.
- 검증 없이 여러 대형 클래스를 한 번에 이동하지 않습니다.
