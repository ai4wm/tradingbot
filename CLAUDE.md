# CLAUDE.md

이 문서는 `trading-bot`의 다음 작업 순서와 구조 개편 원칙을 기록합니다.

## 프로젝트 원칙

- 사용자에게는 존대어로 응답합니다.
- 현재 정상 작동하는 기능을 유지하면서 단계적으로 구조를 분리합니다.
- 구조 개편과 기능 변경을 한 단계에서 함께 처리하지 않습니다.
- 각 단계가 끝날 때 Python 문법 검사, import 검사와 주요 화면 실행 검증을 수행합니다.
- API 키, 계좌 정보, 텔레그램 세션과 `.env`는 Git에 커밋하지 않습니다.
- 실계좌 주문 기능을 변경할 때는 주문 수량, 중복 주문 방지와 비상 정지 동작을 우선 확인합니다.

## 현재 구조 문제

- `main.py`에 앱 제어, 분석창, 뉴스, 상한가, 테마와 시장 분석 기능이 집중되어 있습니다.
- `AnalysisWindow`가 여러 탭의 UI와 수집 로직을 모두 직접 관리합니다.
- LS·네이버 뉴스 로직이 화면 코드와 강하게 연결되어 있어 텔레그램 뉴스를 추가하면 중복 코드가 늘어날 수 있습니다.

## 작업 순서

### 1. 현재 상태 보존

- 미커밋 변경을 확인하고 기능 단위로 커밋합니다.
- 현재 실행 가능한 상태와 주요 설정을 기준점으로 남깁니다.
- 리팩터링 전에 최소한 뉴스 수신, 소리, 조건검색과 주문 관련 점검 경로를 확보합니다.

### 2. 뉴스 계층 분리

다음 구조를 기준으로 새 패키지를 만듭니다.

```text
news/
├─ __init__.py
├─ models.py
├─ repository.py
├─ ls_client.py
├─ naver_client.py
└─ telegram_client.py
```

- `models.py`: 공통 `NewsItem` 데이터 형식
- `repository.py`: 뉴스 저장, 조회와 중복 방지
- `ls_client.py`: LS 실시간 뉴스와 서버 누락분 보완
- `naver_client.py`: 네이버 관심종목 뉴스 수집
- `telegram_client.py`: 텔레그램 채널 과거·실시간 메시지 수집

기존 파일은 한 번에 삭제하지 않고 호출부를 새 구조로 전환한 뒤 제거합니다.

### 3. 분석창 뉴스 탭 분리

```text
ui/
├─ __init__.py
├─ realtime_news_tab.py
├─ stock_news_tab.py
└─ telegram_news_tab.py
```

- LS 실시간 뉴스 탭을 먼저 분리합니다.
- 네이버 종목뉴스 탭을 두 번째로 분리합니다.
- 분리된 탭은 Qt Signal로 `AnalysisWindow`와 통신합니다.
- 상단 전광판, 소리와 관심종목 상태는 공통 이벤트로 전달합니다.
- 탭을 옮기는 단계에서는 화면 모양과 기존 동작을 변경하지 않습니다.

### 4. 텔레그램 뉴스 구현

환경 변수 기본 형태:

```env
TG_API_ID=
TG_API_HASH=
TG_PHONE=
TG_CHANNELS=@stockinfojji,@stock_messenger,@moneythemestock,@YeouidoStory2
```

구현 요구사항:

- `TG_API_ID/TG_API_HASH`를 사용하는 Telegram 사용자 API 방식으로 연결합니다.
- 최초 로그인 시 전화번호 인증번호와 필요한 경우 2단계 인증 비밀번호를 처리합니다.
- 인증 세션 파일은 Git에서 제외합니다.
- 채널별 마지막 메시지 ID를 DB에 저장합니다.
- 앱 실행 시 마지막 ID 이후 게시물을 소급 수집하여 앱 종료 중 누락분을 보완합니다.
- 소급 수집이 끝나면 새 게시물을 실시간 수신합니다.
- 채널 ID와 메시지 ID 조합으로 중복 저장을 방지합니다.
- 공개 채널 또는 로그인 계정이 접근할 수 있는 채널만 처리합니다.
- 삭제된 글이나 Telegram에서 제공하지 않는 과거 글은 복구 대상으로 보지 않습니다.

텔레그램 뉴스 탭 표시 항목:

- 번호
- 게시 시각
- 채널명
- 종목명·종목코드
- 제목 또는 본문 요약
- 원문 링크

필요 동작:

- 채널, 종목명, 종목코드와 본문 검색
- 관심종목만 보기
- 신규 게시물 강조
- 새 게시물 알림음
- 분석창 상단 전광판에 최신 텔레그램 뉴스 표시
- 행 클릭 시 앱 상세보기 또는 Telegram 원문 열기

### 5. 나머지 분석 탭 분리

뉴스 기능이 안정화된 다음 아래 순서로 진행합니다.

1. 상한가 탭
2. 테마 탭
3. 시장·수급 분석 영역
4. 공시와 예측 관련 작업

주문, 잔고와 키움 실시간 등록 코드는 뉴스·분석 화면 분리가 끝난 뒤 별도 단계에서 검토합니다.

### 6. 분석창 시계 분리

- 분석창 상단의 시계만 작은 독립 창으로 분리할 수 있게 합니다.
- 뉴스 전광판은 분리하지 않고 분석창에 그대로 둡니다.
- 시계를 분리하면 뉴스 전광판이 비어 있는 시계 자리까지 차지해 상단 전체 너비로 확장됩니다.
- 분리된 시계 창은 항상 위로 표시하고 마우스로 이동할 수 있게 합니다.
- 분리된 시계의 위치와 분리 상태를 `layout.ini`에 저장합니다.
- 시계가 분리된 상태로 프로그램을 종료했다면 다음 실행 때 저장된 위치에 독립 시계 창을 자동으로 다시 띄웁니다.
- 종료 직전 시계가 분석창에 합쳐진 상태였다면 다음 실행 때도 분석창 안에 표시합니다.
- 분리된 시계를 닫거나 합치기 동작을 실행하면 시계가 분석창의 원래 자리로 돌아옵니다.
- 분석창의 기존 `📌 항상 위` 버튼과 설정은 그대로 유지합니다.
- 분석창의 항상 위 상태와 분리 시계의 항상 위 상태는 서로 영향을 주지 않게 합니다.
- 시계 창은 시각과 KRX·NXT 장 상태만 표시하며 뉴스 수집이나 전광판 상태를 직접 관리하지 않습니다.
- 분리·합치기 전후에 뉴스 전광판의 클릭, 최신 뉴스 강조와 기사 열기 동작이 유지되어야 합니다.

## 공통 뉴스 데이터 권장 형식

```python
NewsItem(
    provider: str,
    external_id: str,
    channel: str,
    published_at: datetime,
    title: str,
    body: str,
    stock_codes: tuple[str, ...],
    url: str,
)
```

- 공급처별 원본 데이터는 클라이언트에서 공통 형식으로 변환합니다.
- UI는 공급처별 API 응답 구조를 직접 알지 않도록 합니다.
- DB 중복키는 `provider + channel + external_id`를 기본으로 사용합니다.

## 단계별 검증

```powershell
.\.venv\Scripts\python.exe -m py_compile config.py api.py ws.py gui.py rank.py main.py analysis_db.py prediction_model.py ls_news_ws.py ls_news_server_sync.py
git diff --check
```

추가로 확인할 항목:

- 분석창을 숨겨도 활성화된 뉴스 알림음이 유지되는지 확인
- LS 종목코드 있음·없음 사운드가 구분되는지 확인
- 네이버 신규 뉴스 사운드와 상단 전광판 표시 확인
- 앱 재시작 후 뉴스 중복 저장 및 누락 보완 확인
- 본창 재조회 체크가 오전 09:02:20에 자동 해제되는지 확인
- 추가 조건검색창의 재조회 체크에는 영향을 주지 않는지 확인

## 하지 말아야 할 작업

- 구조 개편 중 기존 주문 로직을 동시에 재작성하지 않습니다.
- 기존 DB를 삭제하거나 전체 재생성하지 않습니다.
- 사용자 `.env`, `layout.ini`, 운영 DB와 세션 파일을 덮어쓰지 않습니다.
- 리팩터링이 끝나기 전에 기존 파일을 먼저 삭제하지 않습니다.
- 검증 없이 여러 대형 클래스를 한 번에 이동하지 않습니다.
