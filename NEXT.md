# 다음 작업 (현재 상태 & 할 일)

> 최종 업데이트: 2026-07-06. `PLAN.md`는 전체 설계, 이 파일은 "지금 어디까지 됐고 다음에 뭘 하나".

## 지금 되는 것 (검증됨)

- `python main.py` → 창 뜸. 실전 도메인(`api.kiwoom.com`) 토큰 발급 → 웹소켓 LOGIN → 조건식 148개 로딩.
- 조건식 선택 + **등록** → 편입 종목이 그리드에 뜨고, `ka10095` 벌크 조회로
  **종목명·현재가·등락률·거래량·매도/매수잔량**이 한꺼번에 채워짐 (장 마감 후에도).
- 편입 즉시 행 표시 → 0.4초 디바운스 후 1회 벌크 조회 (영웅문과 같은 방식).
- **이탈삭제** 체크 시 이탈 종목 자동 제거. 등락률 헤더 정렬, 빨강/파랑 색.

## 아직 안 되는 것 / 미검증

1. **장중 실시간 틱 미확인** — 0B(체결)/0D(호가) 실시간 갱신은 코드는 있으나
   **장중(09:00~15:30)에 실제로 움직이는지 확인 안 됨.** 다음 개장 때 최우선 확인.
   ⚠️ `ws.py`의 `FID` 매핑(10/12/13/121/125)이 실제 실시간 필드와 맞는지 이때 검증.
2. **업종·전일거래량 칸 비어 있음** — `ka10001`/`ka10095` 응답에 없음. 채우려면 별도 조회 필요.
3. **재접속·PING 30분 방치** 실측 미완 (코드는 있음).

## 다음 할 일 (우선순위 순)

1. **[장중 필수] 실시간 틱 검증** — 개장 후 조건식 등록해서 현재가/등락률이 실시간으로
   움직이는지 확인. 안 움직이면 `ws.py` FID 매핑과 REAL 메시지 구조를 실제 수신값으로 대조.
2. **업종 채우기** — 업종명 주는 TR 찾아 편입 시 벌크 조회에 합치기 (ka10099 종목정보 등 후보).
3. **재접속 실측** — 와이파이 off/on 후 조건검색+시세 자동 재등록 되는지, 30분 방치 PING 유지 확인.
4. 그 다음은 `PLAN.md` §9 백로그 (자동매매/체결통보/다중창 등) — **아직 시작 안 함**.

## 주의

- `config.py`의 `IS_MOCK=False` (실전). **이 화면은 조회 전용이라 안전.**
  자동매매 붙이기 전에 반드시 모의 키로 되돌리고 모의투자에서 검증할 것.
- `.env`에 실전 appkey/secretkey 있음 (git 제외됨). 절대 커밋 금지.
- 로그는 `bot.log`.
